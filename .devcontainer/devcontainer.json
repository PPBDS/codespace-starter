{
  "name": "R Data Science",
  "image": "ghcr.io/rocker-org/devcontainer/tidyverse:4",
  "remoteEnv": {
    "BROWSER": "/usr/bin/true"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "reditorsupport.r",
        "rdebugger.r-debugger",
        "quarto.quarto"
      ],
      "settings": {
        "r.rterm.linux": "/usr/local/bin/radian",
        "r.bracketedPaste": true,
        "r.plot.useHttpgd": true,
        "terminal.integrated.environmentChangesRelaunch": false,
        "workbench.secondarySideBar.defaultVisibility": "hidden",
        "workbench.startupEditor": "none"
      }
    },
    "codespaces": {
      "openFiles": []
    }
  },
  "postCreateCommand": "printf '#!/bin/sh\\nexit 0' | sudo tee /usr/local/bin/xdg-open > /dev/null && sudo chmod +x /usr/local/bin/xdg-open && quarto install tinytex --no-prompt && R -e 'install.packages(\"tutorial.helpers\")'",
  "postAttachCommand": {
    "R": "radian"
  }
}